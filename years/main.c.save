#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i,year;
    i=0;
    year=2000;
    while (year<=2200)
    {
        if((year%4==0&&year%100!=0)||(year%400==0))
        printf("%d ",year);
        year++;
        i++;
    }
    printf
    return 0;
}
