#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char c;
    printf("请输入n个字符组成的字符串（1<=n<=20，以空格分隔）：");
    while ((c=getchar())!='\n')
    int i,len=strlen(c);
    for (i=0;i<len&&str[i]!='\n';i++)
    {
        if (str[i]!=32)
            {
                if ((str[i]>='A'&&str[i]<='V')||(str[i]>='a'&&str[i]<='v'))
                    str[i]=str[i]+4;
                else
                    str[i]=str[i]-22;

            }
        else
            str[i]=32;
    }
    printf("密码为：");
    putchar(str);
    return 0;
}
